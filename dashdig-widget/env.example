# DashDig Widget - CDN Deployment Configuration
# Copy this file to .env.local and fill in your CloudFlare R2 credentials

# ============================================================================
# CloudFlare R2 Configuration (Required)
# ============================================================================

# Get these from: CloudFlare Dashboard → R2 → Manage R2 API Tokens
R2_ACCESS_KEY_ID=your_r2_access_key_id_here
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key_here

# Format: https://<account-id>.r2.cloudflarestorage.com
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# Your R2 bucket name (default: dashdig-widget-cdn)
R2_BUCKET_NAME=dashdig-widget-cdn

# ============================================================================
# CDN Configuration (Optional)
# ============================================================================

# Custom domain for CDN (if configured in CloudFlare)
CDN_CUSTOM_DOMAIN=cdn.dashdig.com

# ============================================================================
# Quick Setup Guide
# ============================================================================
#
# 1. Copy this file:
#    cp env.example .env.local
#
# 2. Get CloudFlare R2 credentials:
#    - Login to CloudFlare Dashboard
#    - Go to R2 → Manage R2 API Tokens
#    - Create token with "Admin Read & Write" permissions
#
# 3. Create R2 bucket:
#    - In CloudFlare Dashboard → R2
#    - Click "Create bucket"
#    - Name it: dashdig-widget-cdn
#
# 4. Fill in the values above
#
# 5. Deploy:
#    npm run deploy
#
# ============================================================================
# Security
# ============================================================================
#
# - NEVER commit .env.local to git (it's in .gitignore)
# - Keep credentials secure
# - Rotate credentials every 90 days
# - Use separate credentials for prod/staging
#
# ============================================================================

